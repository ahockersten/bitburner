// så idén är att bygga ett skript som håller koll på alla servrar
// vi har. Vi kan då räkna ut vad som händer vid varje attack, dispatcha,
// beräkna om, och sen attackera när det är dags. Vi kan också hålla
// koll på flera servrar
import { NS } from "@ns";

/** @param {NS} ns */
export async function main(ns: NS): Promise<void> {
}
